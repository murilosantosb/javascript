function soma(n1=0 , n2=0){  // n1=0 , Seu eu não colocar valores na chamada ele vai valer 0
    return n1 + n2
}

console.log(soma(7))  // N1 + N2 

/* Ser o valor da ação tiver faltando com a do parâmetro o valor vai ser NaN
Só colocar =0 que não da problema  */